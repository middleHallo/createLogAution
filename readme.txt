0、安装node环境
1、将导出的csv文件放入当前文件夹，并将其改成data.csv
2、右键单击start.bat文件，将第一行中的路径改成当前路径
      第一行代码： cd e:\program\myLog （表示的是：用命令进入e盘下的pragram/myLog中）
      如果不会，则将鼠标移到当前文件夹的路径处，复制即可，然后粘贴到start.bat文件中的第一行即可
3、改完保存后退出，然后双击start.bat文件，等待弹窗，会显示今天的pnl确认
4、随便按一下键盘退出即可，当前文件夹会生成一个temp.csv文件